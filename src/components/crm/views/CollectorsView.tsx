"use client";
import { useEffect, useState } from "react";
import { KeyRound, Mail, MapPin, Plus, Power, UsersRound } from "lucide-react";
import { toast } from "sonner";
import { EmptyState, LoadingState, Modal } from "@/components/crm/Modal";
import type { Collector, Zone } from "@/components/crm/types";
import { api } from "@/components/crm/utils";

export function CollectorsView({refreshKey}:{refreshKey:number}){const[collectors,setCollectors]=useState<Collector[]>([]);const[zones,setZones]=useState<Zone[]>([]);const[loading,setLoading]=useState(true);const[open,setOpen]=useState(false);const[saving,setSaving]=useState(false);async function load(){const d=await api<{collectors:Collector[];zones:Zone[]}>("/api/collectors");setCollectors(d.collectors);setZones(d.zones);setLoading(false)}useEffect(()=>{void load()},[refreshKey]);async function create(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);try{const f=new FormData(e.currentTarget);await api("/api/collectors",{method:"POST",body:JSON.stringify(Object.fromEntries(f.entries()))});await load();setOpen(false);toast.success("Cobrador creado. Contraseña: cobro1234*")}catch(e){toast.error(e instanceof Error?e.message:"Error")}finally{setSaving(false)}}async function reset(item:Collector){if(!confirm(`¿Restablecer la contraseña de ${item.name} a cobro1234*?`))return;try{await api(`/api/collectors/${item.id}/reset-password`,{method:"POST"});toast.success("Contraseña restablecida y sesiones cerradas");await load()}catch(e){toast.error(e instanceof Error?e.message:"Error")}}async function toggle(item:Collector){try{await api(`/api/collectors/${item.id}`,{method:"PATCH",body:JSON.stringify({active:!item.active})});await load();toast.success(item.active?"Acceso desactivado":"Acceso activado")}catch(e){toast.error(e instanceof Error?e.message:"Error")}}if(loading)return <LoadingState/>;return <div className="page-stack"><div className="toolbar"><div className="team-summary"><span><UsersRound/></span><div><strong>{collectors.filter((c)=>c.active).length} activos</strong><small>{collectors.length} cuentas creadas</small></div></div><button className="primary-button" onClick={()=>setOpen(true)}><Plus/>Nuevo cobrador</button></div><div className="collector-grid">{collectors.map((item)=><article className={!item.active?"disabled":""} key={item.id}><header><span className="large-avatar">{item.name.slice(0,2).toUpperCase()}</span><div><h3>{item.name}</h3><p><Mail/>{item.email}</p></div><b className={item.active?"status-badge active":"status-badge suspended"}>{item.active?"Activo":"Inactivo"}</b></header><div className="collector-stats"><div><strong>{item._count.assignedClients}</strong><span>Clientes</span></div><div><strong>{item._count.managedCredits}</strong><span>Créditos</span></div></div><p className="collector-zone"><MapPin/>{item.zone?.name||"Sin zona asignada"}</p>{item.mustChangePassword&&<div className="temporary-alert"><KeyRound/>Pendiente de cambiar clave temporal</div>}<footer><button onClick={()=>void reset(item)}><KeyRound/>Restablecer clave</button><button className={item.active?"danger-action":"success-action"} onClick={()=>void toggle(item)}><Power/>{item.active?"Desactivar":"Activar"}</button></footer></article>)}</div>{!collectors.length&&<EmptyState icon={<UsersRound/>} title="Aún no hay cobradores" text="Crea la primera cuenta para comenzar."/>}{open&&<Modal title="Nuevo cobrador" subtitle="Accederá con su correo y cobro1234*" onClose={()=>setOpen(false)}><form className="modal-form" onSubmit={create}><label className="field"><span>Nombre completo</span><input name="name" minLength={3} required autoFocus/></label><label className="field"><span>Correo electrónico</span><input name="email" type="email" required/></label><label className="field"><span>Teléfono</span><input name="phone"/></label><label className="field"><span>Zona principal</span><select name="zoneId"><option value="">Sin zona</option>{zones.map((z)=><option key={z.id} value={z.id}>{z.name}</option>)}</select></label><div className="info-box"><KeyRound/><span>En el primer acceso deberá crear obligatoriamente una contraseña personal.</span></div><div className="form-actions"><button type="button" className="secondary-button" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary-button" disabled={saving}>Crear acceso</button></div></form></Modal>}</div>}
