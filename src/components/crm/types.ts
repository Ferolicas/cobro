export type AppUser = { id: string; name: string; email: string; role: "MASTER" | "COLLECTOR"; mustChangePassword: boolean; active: boolean };
export type Zone = { id: string; name: string };
export type Collector = { id: string; name: string; email: string; phone?: string | null; active: boolean; mustChangePassword: boolean; zone?: Zone | null; _count: { assignedClients: number; managedCredits: number } };
export type Installment = { id: string; number: number; dueDate: string; expectedCents: number; paidCents: number; status: string; paidAt?: string | null };
export type Payment = { id: string; amountCents: number; paidAt: string; method: string; source: string; note?: string | null; collector?: { name: string } | null };
export type StoredDocument = { id: string; category: string; fileName: string; mimeType: string; sizeBytes: number; createdAt: string };
export type Credit = { id: string; code: string; clientId: string; collectorId?: string | null; principalCents: number; interestCents: number; totalDueCents: number; installmentCents: number; disbursedAt: string; maturityDate: string; status: string; microinsuranceCents: number; advancePaymentCents: number; priorSettlementCents: number; cashDeliveredCents: number; paidCents: number; balanceCents: number; daysRemaining: number; dueTodayCents: number; progress: number; notes?: string | null; client: { id: string; name: string; phone?: string | null; businessName?: string | null }; collector?: { id: string; name: string } | null; installments: Installment[]; payments?: Payment[]; documents?: StoredDocument[] };
export type Client = { id: string; code: string; name: string; documentNumber?: string | null; phone?: string | null; alternatePhone?: string | null; businessName?: string | null; businessType?: string | null; address?: string | null; locationNotes?: string | null; reference?: string | null; notes?: string | null; zoneId?: string | null; collectorId?: string | null; riskStatus: string; collector?: { id: string; name: string; email?: string } | null; zone?: Zone | null; credits: Credit[]; documents?: StoredDocument[]; activities?: { id: string; title: string; description?: string | null; createdAt: string; actor?: { name: string } | null }[] };
export type Notification = { id: string; type: string; title: string; message: string; entityType?: string | null; entityId?: string | null; actionUrl?: string | null; details?: Record<string, unknown> | null; readAt?: string | null; createdAt: string; actor?: { id: string; name: string; email: string } | null };
export type DashboardData = { stats: { clients: number; collectors: number; activeCredits: number; overdue: number; activeCapitalCents: number; portfolioCents: number; expectedProfitCents: number; todayDueCents: number; collectedTodayCents: number; unread: number }; urgentCredits: Credit[]; series: { date: string; amountCents: number }[] };
export type Liquidation = { id: string; date: string; openingBaseCents: number; cashOutCents: number; collectedCashCents: number; collectedYapeCents: number; disbursedCents: number; expensesCents: number; collectorWithdrawalCents: number; microinsuranceCents: number; closingCashCents: number; expectedClosingCents: number; differenceCents: number; newClientsCount: number; totalAssignedClients: number; overdue30Count: number; zeroBalanceCount: number; status: string; notes?: string | null; collector: { id?: string; name: string }; documents?: StoredDocument[] };
export type LiquidationSummary = { openingBaseCents: number; previousClosingCents?: number | null; collectedCashCents: number; collectedYapeCents: number; collectedTransferCents: number; collectedDigitalCents: number; totalCollectedCents: number; disbursedCents: number; advancePaymentCents: number; microinsuranceCents: number; renewalSettlementCents: number; cashOutCents: number; expensesCents: number; collectorWithdrawalCents: number; expectedClosingCents: number; totalAssignedClients: number; newClientsCount: number; overdue30Count: number; zeroBalanceCount: number; movementCount: number };
export type AuditEntry = { id: string; action: string; entityType: string; entityId: string; beforeData?: unknown; afterData?: unknown; metadata?: unknown; createdAt: string; actor?: { name: string; email: string } | null };
