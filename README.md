# Cobro CRM

CRM privado de microcréditos diarios para `cobro.olcas.app`.

## Desarrollo

```bash
cp .env.example .env
pnpm install
pnpm db:migrate
pnpm db:seed
pnpm dev
```

La documentación técnica y funcional se encuentra en [`docs/PROJECT-MAP.md`](docs/PROJECT-MAP.md).
